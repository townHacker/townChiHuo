// dependent Backbone.js
(function() {
    var cocoben = new object();
    
    // 定义导航对象 cocoben.NavObject
    cocoben.NavObject = Backbone.Model.extend({
	
    });


    // 定义子导航对象 cocoben.SubNavObject
    cocoben.SubNavObject = Backbone.Model.extend({
	
    });


    // 定义模块对象 cocoben.ModuleObject
    cocoben.ModuleObject = Backbone.Model.extend({
	init: function() {
	    
	},
	refresh: function() {
	    
	}, 
    });


    // 定义模块对象集合 cocoben.ModuleObjects[]
    cocoben.ModuleObjects = Backbone.Collection.extend({
	
    });


    // 定义编辑对象 cocoben.EditObject
    cocoben.EditObject = Backbone.Model.extend({
	
    });
})()
